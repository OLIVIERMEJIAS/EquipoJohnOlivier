let num1 = 0;
let num2 = 0;


function sumar(num){
  num1 = document.getElementById('num1Sum').value;
  num2 = docuemq.getElementById('num2Sum').value;

  introducirNumero(num,num1);
  introducirNumero(num,num2);
}

function introducirNumero(nummber,contenedor){
    if (contenedor='') {
        contenedor = number;
    }else{
        contenedor = contenedor += number
    }
}